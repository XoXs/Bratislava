# Theme

CSS approach: vanilla CSS in `src/styles.css`.

Design language: warm editorial travel planner with card-based board layout, compact icon actions, soft shadows, clean Inter/system typography, and category badges.

Relevant full source files:

- `src/styles.css`
- `src/App.tsx`
- `src/data.ts`

```css
/* Full theme source should be passed as context file: src/styles.css */
```
