# Layouts

The app uses a single-page layout in `src/App.tsx`. There are no separate layout components.

## `src/App.tsx`

Description: Root application shell with sticky topbar, overview metrics, tab navigation, planning board, panels, login screen, and modal.

```tsx
// Full source should be passed as a context file: src/App.tsx
```
